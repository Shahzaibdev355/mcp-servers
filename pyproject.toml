[project]
name = "mcp-servvers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Shahzaib_Dev", email = "shahzaibsheikh355@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "langchain-ollama>=1.1.0",
    "mcp<2.0.0",
    "mcp-use>=1.7.0",
    "praisonaiagents[llm]>=1.6.167",
]

[project.scripts]
mcp-servvers = "mcp_servvers:main"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
